# Shared MCP templates (`~/.ai/mcp`)

Store non-secret MCP configuration templates here.

These files are synchronized to:

- `~/.claude/mcp`
- `~/.codex/mcp`

Do not store plaintext credentials in this directory.
