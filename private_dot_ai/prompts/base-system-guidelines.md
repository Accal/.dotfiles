# Base System Guidelines

- Prefer deterministic and idempotent workflows.
- Separate shared assets from agent runtime state.
- Keep secrets out of version control.
- Validate changes with small, repeatable checks.
